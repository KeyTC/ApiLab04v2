apiVersion: v1
kind: Pod
metadata:
  name: app-pod
  labels:
    app: app
    type: mvc
spec:
  containers:
    - name: app-pod
      image: keylor/apilab04:v2
      ports:
        - containerPort: 8080